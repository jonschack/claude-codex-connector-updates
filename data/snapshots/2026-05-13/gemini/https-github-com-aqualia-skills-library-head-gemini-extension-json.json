{
  "name": "sharepoint-audit",
  "version": "0.1.0",
  "description": "Run a SharePoint permission audit using a local PowerShell script and Python orchestrator.",
  "commands": {
    "audit": {
      "description": "Run a SharePoint audit for a single site or CSV list.",
      "shell": true,
      "command": "python ./sharepoint-audit-agent/agent/python/audit_agent.py --tenant-id \"{{tenant_id}}\" --app-id \"{{app_id}}\" --pfx-path \"{{pfx_path}}\"{{#site_url}} --site-url \"{{site_url}}\"{{/site_url}}{{#csv}} --csv \"{{csv}}\"{{/csv}}{{#internal_domains}} --internal-domains \"{{.}}\"{{/internal_domains}} --output ./runs"
    }
  }
}
