{
  "name": "goindexer",
  "version": "1.0.0",
  "description": "Semantic code search and codebase understanding via MCP. Uses tree-sitter chunking, Ollama embeddings, and SQLite vector search.",
  "contextFileName": "GEMINI.md",
  "settings": [
    {
      "name": "Embedding model",
      "description": "Ollama embedding model. (I recommend qwen3-embedding:4b running on laptops.)",
      "envVar": "SOURCE_INDEX_EMBED_MODEL"
    },
    {
      "name": "Ollama URL",
      "description": "Ollama API URL (paste the default: http://localhost:11434)",
      "envVar": "SOURCE_INDEX_OLLAMA_URL"
    }
  ],
  "mcpServers": {
    "goindexer": {
      "command": "${extensionPath}${/}bin${/}goindexer",
      "args": ["--path", "${workspacePath}", "--watch"],
      "cwd": "${workspacePath}"
    }
  }
}
