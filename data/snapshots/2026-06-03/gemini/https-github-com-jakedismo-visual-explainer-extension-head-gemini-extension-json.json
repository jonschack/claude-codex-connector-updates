{
  "name": "visual-explainer-extension",
  "version": "1.0.0",
  "description": "Generate beautiful, self-contained HTML pages that visually explain systems, code changes, plans, and data. Now with native AI image generation.",
  "contextFileName": "GEMINI.md",
  "mcpServers": {
    "visual-explainer-mcp": {
      "command": "node",
      "args": ["dist/index.js"],
      "env": {
        "NANOBANANA_GEMINI_API_KEY": "${NANOBANANA_GEMINI_API_KEY}"
      }
    }
  },
  "settings": [
    {
      "name": "api_key",
      "description": "Your Gemini Nanobanana API Key for image generation",
      "envVar": "NANOBANANA_GEMINI_API_KEY",
      "sensitive": true
    }
  ]
}
